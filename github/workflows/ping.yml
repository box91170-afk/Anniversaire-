name: Activation quotidienne

on:
  schedule:
    # Se lance tous les jours à 08:00 UTC (9h ou 10h en France selon l'heure d'été/hiver)
    - cron: '0 8 * * *'
  workflow_dispatch: # Permet de le tester manuellement

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ouvrir le lien
        run: |
          curl -I -s https://box91170-afk.github.io/Anniversaire-/
          
